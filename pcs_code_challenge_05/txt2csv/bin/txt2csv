#!/usr/bin/env ruby
require 'csv'

class Analyze
  def initialize(input_file, output_file)
    @input = File.new(input_file, )
    @output = File.new(output_file, )

  def analyze

  def pattern_choice
  end
end
